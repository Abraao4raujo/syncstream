import useFetch from "../adapters/useFetch";
import HorizontalCards from "../components/containerCards/HorizontalCards.jsx";

const Series = () => {
  return <HorizontalCards format={"tv"} page={"1"} />;
};

export default Series;
